# —— 强制走 Inductor（否则默认 Eager 不会触发 AOT）
# PYTORCH_DYNAMO_BACKEND=inductor

# —— 打开 Triton + ATEN + CPP（CPU/GPU 都允许）
# TORCHINDUCTOR_CONFIG={"max_autotune_gemm_backends":["TRITON","ATEN","CPP"]}

# —— 单卡也走分布式接口，保持脚本里 init_dist_and_get_args 不改
PYDEVD_CONTAINER_RANDOM_ACCESS_MAX_ITEMS=2000

USE_DISTRIBUTED=1
WORLD_SIZE=1
RANK=0
LOCAL_RANK=0

# 线程 / NCCL / wandb
OMP_NUM_THREADS=8
NCCL_IB_DISABLE=0
NCCL_IB_GID_INDEX=3
NCCL_SOCKET_IFNAME=lo
WANDB_MODE=offline
